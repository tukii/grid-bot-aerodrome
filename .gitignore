# Secretos — NUNCA subir
.env
*.env
.env.*
!.env.example

# Entorno virtual y dependencias
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Datos de trading (DB, logs, backups, estado)
data/
*.db
*.log
config.backup*.yaml

# Config con tokens (el token de Telegram está en config.yaml)
config.yaml

# Backups de código
*.bak.*
*.bak
*.backup.*

# Sistema
.DS_Store
*.swp
